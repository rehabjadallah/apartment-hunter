export function buildEnvironment(urls) {
    return {
        cloudUrl: urls.cloudUrl,
        basePath: new URL(urls.siteUrl).pathname || "/",
    };
}
export function buildEnvironmentChanged(before, after) {
    const previous = buildEnvironment(before);
    const next = buildEnvironment(after);
    return (previous.cloudUrl !== next.cloudUrl || previous.basePath !== next.basePath);
}
//# sourceMappingURL=deployEnvironment.js.map