/**
 * Parse an ISO timestamp into ms-since-epoch. Returns `Date.now()` on
 * malformed input — `Date.parse` returns `NaN` for bad strings, and `NaN`
 * passes `v.number()` validation, so unguarded calls would silently store
 * `NaN` in the database.
 */
export declare function parseTimestamp(input: string): number;
export declare class AgentMailApiError extends Error {
    status: number;
    body: string;
    permanent: boolean;
    constructor(status: number, body: string, permanent: boolean);
}
type FetchOptions = {
    method: "GET" | "POST" | "PATCH" | "DELETE";
    body?: unknown;
    query?: Record<string, string | number | boolean | undefined | null>;
};
export declare function agentmailFetch(path: string, opts: FetchOptions): Promise<unknown>;
export declare function stripUndefined<T>(value: T): T;
export {};
//# sourceMappingURL=utils.d.ts.map