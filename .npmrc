# The Convex Auth preview is a tarball URL dependency (see README). npm 12
# defaults allow-remote to "none", which fails `npm ci` with EALLOWREMOTE.
# "root" permits only URLs declared in this project's own package.json.
allow-remote=root
